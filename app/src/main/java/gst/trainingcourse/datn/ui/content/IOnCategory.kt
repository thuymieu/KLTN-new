package gst.trainingcourse.datn.ui.content

import gst.trainingcourse.datn.model.Category
import gst.trainingcourse.datn.model.Product

interface IOnCategory {
    fun onImageCategory(cate: Category)
}